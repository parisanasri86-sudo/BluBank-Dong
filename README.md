# BluBank Dong — Shared Money Hub (UX Case Study)

A single-page UX case study for **blu**Bank's Dong (Shared Money Hub) feature.

## Contents

```
.
├── index.html   # The full case study (self-contained, works offline, all assets inlined)
└── README.md
```

## View locally

Just open `index.html` in any browser — no build step, no dependencies.

```bash
# or serve it
python3 -m http.server
# then open http://localhost:8000
```

## Publish with GitHub Pages

1. Push this folder to a GitHub repo.
2. Repo **Settings → Pages → Build and deployment**.
3. Source: **Deploy from a branch**, Branch: `main` / root.
4. Your case study goes live at `https://<username>.github.io/<repo>/`.
